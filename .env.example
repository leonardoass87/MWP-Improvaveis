# Database Configuration - PostgreSQL com Prisma
# Para produção na Vercel, use Vercel Postgres ou outro provedor PostgreSQL
# Formato: postgresql://username:password@host:port/database?schema=public
DATABASE_URL="postgresql://username:password@localhost:5432/bjj_academy?schema=public"

# Para Vercel Postgres, a URL será fornecida automaticamente
# POSTGRES_URL="postgres://default:password@host:5432/verceldb"
# POSTGRES_PRISMA_URL="postgres://default:password@host:5432/verceldb?pgbouncer=true&connect_timeout=15"
# POSTGRES_URL_NON_POOLING="postgres://default:password@host:5432/verceldb"

# JWT Secret
# IMPORTANTE: Gere uma chave secreta forte para produção
# Você pode usar: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# NextAuth Configuration
# Para produção, use o domínio real da aplicação
NEXTAUTH_URL=https://your-app-domain.vercel.app
NEXTAUTH_SECRET=your-nextauth-secret-key

# Vercel Configuration (opcional)
# Essas variáveis são automaticamente definidas pela Vercel
# VERCEL_URL=your-app-domain.vercel.app
# VERCEL_ENV=production